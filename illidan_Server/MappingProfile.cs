using AutoMapper;
using illidan_Shared;

namespace EQ_Server
{
    public class MappingProfile : Profile
    {
        public MappingProfile()
        {
            //CreateMap<illidan_Server.Models.User, Number>()
            //    .ForMember(dest => dest.Value, opt => opt.MapFrom(src => src.Number));

        }
    }
}