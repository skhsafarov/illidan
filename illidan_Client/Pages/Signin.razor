@page "/signin"

#if DEBUG
@for (int i = 0; i < 80; i++)
{
    <img style="background-color: var(--color@( ((i % 11) + 1).ToString() ))" src="/images/queue images/image@((i+1).ToString()).png" />
}
#else
@for (int i = 0; i < 80; i++)
{
    <img style="background-color: var(--color@( ((i % 11) + 1).ToString() ))" src="/illidan/images/queue images/image@((i+1).ToString()).png" />
}
#endif