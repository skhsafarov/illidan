@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/signin/{Navigation.ToBaseRelativePath(Navigation.Uri)}");
    }
}
